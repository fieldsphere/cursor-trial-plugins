# Design Language

This file intentionally does not define a default design language for every team.

Document your product's visual hierarchy, layout rules, and accessibility expectations in `design-schema.md` instead. When the schema is incomplete, keep wireframes and mockups neutral and call out the missing decisions rather than inventing them.